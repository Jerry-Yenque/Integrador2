NAVIGATION COMPOSE:
    Allow us to navigate across the screens(added in ep. 12 min 3:01)
    This line goes on build.gradle(Module:app) inside dependencies
        implementation("androidx.navigation:navigation-compose:2.7.0")

DAGGER HILT:
    This line goes on build.gradle(Project:name) inside plugins
        id 'com.google.dagger.hilt.android' version '2.44' apply false

    This line goes on build.gradle(Project:app) inside plugins
        id 'kotlin-kapt'
        id 'com.google.dagger.hilt.android'
    This on dependencies
        implementation "com.google.dagger:hilt-android:2.44"
        kapt "com.google.dagger:hilt-compiler:2.44"
    Module scope bottom
        kapt {
            correctErrorTypes true
        }

HIL NAVIGATION COMPOSE:
    This line goes on build.gradle(Project:app) inside depencies
    implementation "androidx.navigation:navigation-compose:2.5.3"